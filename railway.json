{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && cp public/the_mog_effect_blueprint.pdf dist/public/"
  },
  "deploy": {
    "startCommand": "node dist/index.cjs",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
